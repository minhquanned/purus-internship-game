{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "playcanvas": "^2.1.0",
    "rollup-plugin-delete": "^2.1.0",
    "vite": "^5.4.19"
  },
  "scripts": {
    "start": "vite serve --open --host",
    "build": "vite build",
    "serve": "vite preview",
    "preview": "vite preview --port 9000",
    "predeploy": "npm run build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "ammo.js": "github:kripken/ammo.js"
  }
}
